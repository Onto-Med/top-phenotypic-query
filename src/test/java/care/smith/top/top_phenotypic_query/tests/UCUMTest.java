/*
package care.smith.top.top_phenotypic_query.tests;

import static org.junit.jupiter.api.Assertions.assertEquals;

import java.math.RoundingMode;

import org.junit.jupiter.api.Test;

import care.smith.top.top_phenotypic_query.ucum.UCUM;

public class UCUMTest extends AbstractTest {

  @Test
  public void test1() {
    assertEquals(
        Double.valueOf(1.8),
        UCUM.convert(Double.valueOf(180), "cm", "m").setScale(1, RoundingMode.HALF_UP));
  }

  @Test
  public void test2() {
    assertEquals(
        Double.valueOf(180.0),
        UCUM.convert(Double.valueOf(1.8), "m", "cm").setScale(1, RoundingMode.HALF_UP));
  }

  @Test
  public void test3() {
    assertEquals(
        Double.valueOf(1.8),
        UCUM.convert(Double.valueOf(1.8), "m", null).setScale(1, RoundingMode.HALF_UP));
  }
}
*/